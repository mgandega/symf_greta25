{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}

{# <div class="container"> #}
    {{ parent() }}
<div class="row">
<div class="col-sm-3">
</div>
<div class="col-sm-9">
<table class="table">
    {{ dump(panier) }}
    <tr><th>Titre</th><th>Quantité</th><th>description</th><th>Prix</th><th>Prix Total</th></tr>
    {% for cle, value in panier.conferenceId %}
        <tr><td>{{ panier.titre[cle] }}</td><td>{{ panier.quantite[cle] }}</td><td>{{ panier.description[cle] }}</td><td>{{panier.prix[cle]}}</td><td>{{ panier.prix[cle]*panier.quantite[cle] }}</td></tr>
    {% endfor %}

</table>
</div>
</div>
{# </div> #}

{% endblock %}

