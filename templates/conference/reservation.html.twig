{% extends 'base.html.twig' %}

{% block title %}Hello ConferenceController!{% endblock %}

{% block body %}

<div class="container">
<div class="row">
    <div class="col-sm-3">
    {# <ul>
    {% for categorie in categories %}
    <li><a href="{{path('app_conference.categorie', {'nom': categorie.nom }) }}">{{ categorie.nom }}</a></li>
    {% endfor %}
    </ul> #}
    </div>
{% for reservation in reservations %}
    <div class="col-sm-3">

    <h5>{{ reservation.id}}</h5>
    <p>{{ reservation.contenu}}</p>
    <a href="{{ path('app_reservation.delete', {'id':reservation.id})}}">suppression</a>
    </div>
{% endfor %}
</div
</div>
{% endblock %}
