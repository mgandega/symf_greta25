{% extends 'base.html.twig' %}

{% block title %}Hello ConferenceController!{% endblock %}

{% block body %}
<div class="container">
<div class="row">
    <div class="col-sm-2"></div>
    <div class="example-wrapper col-sm-6">
    {# novalidate permet de ne pas executer la validation de html #}
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
</div>
{% endblock %}
